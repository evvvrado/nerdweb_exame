import Page from "../classes/Page.mjs"
import Splash from '../components/Splash.mjs'

export default class Blank extends Page {
	constructor() {
		super({
		})
	}


	mount() {
		// this.splash = new Splash('.splash')
		// this.splash.hide()
	}
}